
const Stats = () => {
    return (
      <section className='section'>
        <h2>Stats</h2>
      </section>
    );
  };
// As we have no nested route under stats page, we need not use Outlet component

  export default Stats;